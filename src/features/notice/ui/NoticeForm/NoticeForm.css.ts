import { style } from '@vanilla-extract/css';

export const noticeFormCard = style({
	maxWidth: '600px',
	margin: '0 auto',
});

export const formActions = style({
	marginTop: '24px',
	textAlign: 'right',
});
